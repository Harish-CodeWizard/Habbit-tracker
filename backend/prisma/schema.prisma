generator client {
	provider = "prisma-client-js"
}

datasource db {
	provider = "sqlite"
	url      = env("DATABASE_URL")
}

model User {
	id        Int      @id @default(autoincrement())
	name      String
	email     String   @unique
	password  String
	habits    Habit[]
	createdAt DateTime @default(now())
}

model Habit {
	id          Int      @id @default(autoincrement())
	title       String
	description String?
	streak      Int      @default(0)
	completed   Boolean  @default(false)
	percentCompleted Int @default(0) // 0-10 score for completion
	reminders   Json? // store reminders as JSON (e.g., {"2026-05-26": true})

	userId Int
	user   User @relation(fields: [userId], references: [id])

	createdAt DateTime @default(now())
}
